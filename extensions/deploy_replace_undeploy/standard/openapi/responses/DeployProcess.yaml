description: the process is deployed
headers:
  Location:
    schema:
      type: string
    description: URL to fetch the processDescription of the deployed process
content:
  application/json:
    schema:
      content:
        application/json:
      schema:
        $ref: "https://schemas.opengis.net/ogcapi/processes/part1/1.0/openapi/schemas/swagger/process.yaml"
    examples:
      processDecription:
        summary: A Process Description example
        externalValue: "https://schemas.opengis.net/ogcapi/processes/part1/1.0/examples/json/ProcessDescription.json"
